# تقرير تحليل هيكلية قاعدة بيانات Omar Clinic Pro

**التاريخ:** 18 يونيو 2026
**المؤلف:** Manus AI

## 1. مقدمة

يهدف هذا التقرير إلى تقديم تحليل شامل لهيكلية قاعدة بيانات مشروع Omar Clinic Pro. يركز المشروع على إدارة العيادات الطبية، ويتميز بتصميم متعدد المستأجرين (multi-tenant) لدعم عدة عيادات بشكل مستقل. تم تحليل ملفات الترحيل (migrations) وملف مراجعة المخطط (SCHEMA_PRODUCTION_REVIEW.md) لفهم الجداول، العلاقات، الميزات المعمارية، والتحسينات التي تم تطبيقها لضمان جاهزية النظام للإنتاج.

## 2. نظرة عامة على قاعدة البيانات

تعتمد قاعدة البيانات على نظام PostgreSQL وتستخدم امتدادات مثل `uuid-ossp` لإنشاء معرفات فريدة عالميًا (UUIDs) و `pgcrypto` لوظائف التشفير. تم تصميم القاعدة لتكون قوية وقابلة للتوسع، مع التركيز على الأمان وتتبع التغييرات.

## 3. تحليل الجداول الأساسية

تتكون قاعدة البيانات من عدة جداول رئيسية، كل منها يخدم غرضًا محددًا في إدارة العيادة. فيما يلي تفصيل للجداول الرئيسية وعلاقاتها:

| الجدول | الوصف | المفتاح الأساسي | المفاتيح الخارجية | ملاحظات رئيسية |
|---|---|---|---|---|
| `roles` | تعريف أدوار المستخدمين (مثل المسؤول، مالك العيادة، الطبيب، الموظف، المريض). | `id` (UUID) | لا يوجد | يحتوي على صلاحيات `JSONB` وقيد `CHECK` على أسماء الأدوار. |
| `clinics` | الكيان الرئيسي للعيادة، يمثل كل عيادة في النظام. | `id` (UUID) | لا يوجد | يحتوي على تفاصيل العيادة، حالة الاشتراك، وقيود فريدة على البريد الإلكتروني ورقم الترخيص. |
| `users` | مستخدمو النظام (المسؤولون، مالكو العيادات، الموظفون). | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `role_id` (يشير إلى `roles.id`) | مرتبط بنظام مصادقة Supabase (`auth.users.id`). |
| `doctors` | سجلات الأطباء. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `user_id` (يشير إلى `users.id`) | يحتوي على تخصصات، رقم ترخيص فريد، ومعلومات الاتصال. |
| `patients` | سجلات المرضى. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`) | يحتوي على معلومات شخصية، تاريخ طبي، ومعلومات اتصال للطوارئ. |
| `appointments` | جدولة المواعيد. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `patient_id` (يشير إلى `patients.id`)، `doctor_id` (يشير إلى `doctors.id`) | يحتوي على وقت البدء والانتهاء، سبب الزيارة، والحالة. |
| `treatments` | سجلات العلاج. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `appointment_id` (يشير إلى `appointments.id`)، `patient_id` (يشير إلى `patients.id`)، `doctor_id` (يشير إلى `doctors.id`) | يحتوي على التشخيص، خطة العلاج، الأدوية، والإجراءات. |
| `invoices` | سجلات الفواتير. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `patient_id` (يشير إلى `patients.id`)، `appointment_id` (يشير إلى `appointments.id`) | يحتوي على تفاصيل المبلغ، حالة الدفع، وتواريخ الاستحقاق. |
| `payments` | سجلات الدفع. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `invoice_id` (يشير إلى `invoices.id`)، `patient_id` (يشير إلى `patients.id`) | يحتوي على طريقة الدفع، المبلغ، ورقم المعاملة. |
| `subscriptions` | إدارة اشتراكات العيادات. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`) | يحتوي على تفاصيل الخطة، الحالة، تواريخ البدء والانتهاء، وتفاصيل الفواتير. |
| `activity_logs` | سجلات التدقيق لتتبع الأنشطة. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `user_id` (يشير إلى `users.id`) | يسجل الإجراءات (إنشاء، تحديث، حذف) على الجداول الأخرى. |
| `medical_files` | بيانات تعريف الملفات الطبية (PDF، صور، أشعة سينية). | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `patient_id` (يشير إلى `patients.id`)، `appointment_id` (يشير إلى `appointments.id`)، `uploaded_by` (يشير إلى `users.id`) | يخزن اسم الملف، نوعه، عنوان URL، وحجمه. |
| `prescriptions` | سجلات الوصفات الطبية. | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `patient_id` (يشير إلى `patients.id`)، `doctor_id` (يشير إلى `doctors.id`)، `appointment_id` (يشير إلى `appointments.id`) | يحتوي على التشخيص، الأدوية، التعليمات، والحالة. |
| `inventory_items` | عناصر المخزون (الأدوية، المستلزمات). | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`) | يحتوي على الاسم، الفئة، مستوى المخزون، وسعر الوحدة. |
| `inventory_transactions` | معاملات المخزون (إدخال/إخراج المخزون). | `id` (UUID) | `clinic_id` (يشير إلى `clinics.id`)، `item_id` (يشير إلى `inventory_items.id`)، `performed_by` (يشير إلى `users.id`) | يسجل نوع المعاملة، الكمية، والسبب. |

## 4. الميزات المعمارية الرئيسية

تم تصميم قاعدة البيانات مع مراعاة العديد من الميزات المعمارية الهامة:

### 4.1. تعدد المستأجرين (Multi-Tenancy)

تستخدم معظم الجداول عمود `clinic_id` لربط البيانات بعيادة معينة، مما يضمن عزل البيانات بين العيادات المختلفة. تم تطبيق قيود فريدة على مستوى العيادة (clinic-scoped unique constraints) على حقول مثل `patient_id_number` و `appointment_number` و `invoice_number` و `payment_number` لضمان التفرد داخل كل عيادة بدلاً من التفرد العالمي [1].

### 4.2. الحذف الناعم (Soft Deletion)

تحتوي جميع الجداول تقريبًا على عمود `deleted_at` من نوع `TIMESTAMP WITH TIME ZONE`. هذا يسمح بالحذف الناعم للسجلات، حيث لا يتم حذف البيانات فعليًا من قاعدة البيانات، بل يتم وضع علامة عليها كـ 
محذوفة. هذا يحافظ على سلامة البيانات ويسهل استعادتها إذا لزم الأمر.

### 4.3. تكامل Supabase Auth

تم ربط جدول `users` مباشرة بـ `auth.users(id)` من Supabase Auth [1]. هذا يضمن تكاملاً سلسًا مع نظام المصادقة، مما يسهل إدارة المستخدمين وتطبيق سياسات أمان الصفوف (RLS) باستخدام `auth.uid()`.

### 4.4. سجلات التدقيق (Audit Logs)

يحتوي النظام على جدول `activity_logs` لتتبع جميع الإجراءات (إنشاء، تحديث، حذف) التي تتم على الجداول الرئيسية [1]. يتم استخدام دالة `audit_trigger_function()` لمنع التكرار وضمان تسجيل دقيق للأنشطة، بما في ذلك `clinic_id` و `user_id` ونوع الكيان والقيم القديمة والجديدة.

### 4.5. تحديث الطوابع الزمنية التلقائي (`updated_at` Triggers)

تم تطبيق مشغلات (triggers) تلقائية على معظم الجداول لتحديث عمود `updated_at` تلقائيًا عند كل عملية تحديث للصف [1]. هذا يضمن دقة الطوابع الزمنية ويقلل من الحاجة إلى تحديثها يدويًا من خلال رمز التطبيق.

### 4.6. سياسات أمان الصفوف (Row Level Security - RLS)

تم تمكين RLS على جميع الجداول لفرض عزل البيانات بناءً على `clinic_id` ودور المستخدم [1]. تم تعريف دوال مساعدة مثل `get_user_clinic_id()` و `get_user_role()` و `is_super_admin()` لتبسيط تطبيق هذه السياسات. تضمن هذه السياسات أن المستخدمين يمكنهم فقط الوصول إلى البيانات التي يخول لهم الوصول إليها.

## 5. التحسينات والتعديلات الهامة

تم إجراء العديد من التحسينات والتعديلات على المخطط الأصلي لتعزيز الأمان والأداء وقابلية الصيانة:

*   **إعادة هيكلة جدول `users`**: تم ربط جدول `users` مباشرة بـ `auth.users(id)` في Supabase، مما يوفر تكاملاً أفضل للمصادقة [1].
*   **إزالة تكرار بيانات الاشتراك**: تم إزالة أعمدة الاشتراك المكررة من جدول `clinics`، وجعل جدول `subscriptions` هو المصدر الوحيد للحقيقة لبيانات الاشتراك [1]. تم إنشاء عرض `clinic_subscription_status` لتسهيل الوصول السريع إلى حالة الاشتراك لكل عيادة.
*   **قيود فريدة متعددة المستأجرين**: تم تغيير العديد من القيود الفريدة من عالمية إلى نطاق العيادة (clinic-scoped) لـ `patient_id_number`، `appointment_number`، `invoice_number`، و `payment_number` [1].
*   **إضافة جداول جديدة**: تم إضافة جداول `medical_files`، `prescriptions`، `inventory_items`، و `inventory_transactions` لتوسيع وظائف النظام [2] [3].
*   **إضافة أعمدة Clerk ID و Shopify Subscription ID**: تم إضافة `clerk_id` إلى جدولي `patients` و `doctors` لربطهم ببوابة Clerk، و `shopify_subscription_id` إلى جدول `subscriptions` لتتبع اشتراكات Shopify [3].
*   **فهارس الأداء الإضافية**: تم إضافة فهارس مركبة لتحسين أداء الاستعلامات الشائعة [1].

## 6. الخلاصة

يمثل مخطط قاعدة بيانات Omar Clinic Pro نظامًا قويًا ومصممًا جيدًا لإدارة العيادات الطبية. مع التركيز على تعدد المستأجرين، الأمان من خلال RLS، وتتبع الأنشطة عبر سجلات التدقيق، يوفر المخطط أساسًا متينًا لتطبيق قابل للتوسع وموثوق. التحسينات الأخيرة تعالج المشكلات الحرجة وتضيف وظائف أساسية، مما يجعله جاهزًا للإنتاج.

## 7. المراجع

[1] `omar-clinic-pro/database/migrations/004_v2_production_fixes_final.sql`
[2] `omar-clinic-pro/database/migrations/001_initial_schema.sql`
[3] `omar-clinic-pro/database/migrations/006_missing_tables_and_columns.sql`
[4] `omar-clinic-pro/docs/SCHEMA_PRODUCTION_REVIEW.md`
