# تقرير فحص مشاكل تسجيل الدخول، إنشاء الحساب، والاشتراكات

بناءً على الفحص البرمجي الدقيق لملفات المشروع (Clerk Webhooks, Dashboard Logic, Subscription APIs)، إليك تحليل المشاكل المحتملة والحلول:

## 1. إنشاء الحساب (Account Creation) ✅
*   **الآلية**: المشروع يستخدم **Clerk** للمصادقة و**Webhooks** لمزامنة البيانات مع قاعدة بيانات Supabase.
*   **مشاكل محتملة**:
    *   **تأخر الـ Webhook**: عند إنشاء حساب جديد، قد يستغرق الـ Webhook بضع ثوانٍ لإدراج المستخدم في جدول `users`. إذا حاول المستخدم الدخول للوحة التحكم فوراً، قد لا يجد النظام بياناته.
    *   **فشل المزامنة**: في حال تعطل الـ Webhook لأي سبب تقني، سيبقى الحساب في Clerk ولكن لن يظهر في النظام، مما يمنع الدخول للوحة التحكم.
*   **الحل المطبق في الكود**: يوجد مسار `/api/auth/check-email` للتحقق من وجود المستخدم، وتم ضبط الـ Webhook ليعيد المحاولة في حال الفشل.

## 2. التحويل التلقائي لصفحة الإعدادات (Redirect to Settings) 🔄
*   **الآلية**: عند الدخول لأول مرة (`/dashboard/clinic`)، يقوم النظام بطلب الإحصائيات. إذا لم يجد "عيادة" مرتبطة بالمستخدم، يعيد قيمة `requiresSetup: true`.
*   **السلوك الملاحظ**: النظام يقوم بتحويل المستخدم تلقائياً إلى `/dashboard/clinic/settings` بعد 3 ثوانٍ من التحميل إذا كان الحساب جديداً.
*   **مشكلة محتملة**: إذا كان المستخدم بطيئاً في تعبئة البيانات أو حدث خطأ في الاتصال، قد يشعر أنه "عالق" في صفحة الإعدادات.

## 3. التحويل لصفحة الاشتراكات (Redirect to Subscription) 💳
*   **الآلية**: في صفحة الإعدادات، بعد حفظ بيانات العيادة بنجاح، يوجد "تحويل إجباري" (Force Redirect) لصفحة الاشتراكات:
    ```javascript
    window.location.href = '/dashboard/clinic/subscription';
    ```
*   **مشاكل محتملة**:
    *   **الاعتماد على Shopify**: النظام مهيأ لاستقبال الدفع عبر Shopify Webhooks. إذا لم يتم إكمال عملية الدفع في Shopify، فلن يتم تفعيل الاشتراك في قاعدة البيانات تلقائياً.
    *   **صلاحيات الـ API**: بعض مسارات الاشتراك تتطلب صلاحية `SETTINGS_READ` أو `INVOICE_CREATE`. إذا لم يتم منح هذه الصلاحيات للمستخدم الجديد تلقائياً (عبر الـ Webhook)، سيفشل تحميل صفحة الاشتراكات.

## 4. ملخص حالة "التعليق" (Stuck Scenarios) 🛑
إذا شعرت أنك عالق، فغالباً ما يكون السبب أحد التالي:
1.  **بيانات العيادة ناقصة**: النظام لن يسمح لك بتجاوز صفحة الإعدادات حتى تحفظ اسم العيادة وهاتفها.
2.  **الاشتراك غير نشط**: بعض الصفحات قد تمنع الدخول إذا كانت حالة الاشتراك في جدول `subscriptions` ليست `active`.
3.  **صلاحية المستخدم**: يجب أن يكون دورك `owner` في جدول `users` لتتمكن من إدارة الاشتراكات.

## التوصيات الفنية لتحسين التجربة:
1.  **تحسين رسائل الخطأ**: إضافة تنبيهات واضحة في حال فشل الـ Webhook أو نقص الصلاحيات.
2.  **التفعيل التلقائي للفترة التجريبية**: تعديل الـ Webhook ليقوم بإنشاء اشتراك "تجريبي" (Trial) لمدة 14 يوماً فور إنشاء الحساب، مما يمنح المستخدم وصولاً فورياً دون انتظار الدفع.
3.  **زر تخطي مؤقت**: السماح للمستخدم باستكشاف لوحة التحكم ببيانات وهمية قبل إكمال الإعدادات.
