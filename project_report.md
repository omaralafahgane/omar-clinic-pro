# تقرير تحليل مشروع Omar Clinic Pro

## 1. مقدمة

يهدف هذا التقرير إلى تقديم تحليل شامل لمشروع `Omar Clinic Pro`، وهو نظام لإدارة العيادات. يغطي التقرير هيكلية المشروع، التقنيات المستخدمة، وتحليل مفصل لقاعدة البيانات (Schema) لفهم كيفية عمل النظام والاستعداد للمراحل التالية من التطوير.

## 2. نظرة عامة على هيكلية المشروع

تم استنساخ المشروع `omaralafahgane/omar-clinic-pro` من GitHub. يتبع المشروع هيكلية منظمة تشير إلى تطبيق ويب مبني باستخدام `Next.js`، مع فصل واضح بين المكونات، صفحات التطبيق، ومجلدات قاعدة البيانات. الهيكل الرئيسي للمشروع يتضمن:

*   `app/`: يحتوي على صفحات التطبيق الرئيسية ونقاط نهاية الـ API.
    *   `api/`: نقاط نهاية الـ API للعيادات، لوحة التحكم، التقارير، الاشتراكات، و webhooks.
    *   `dashboard/`: صفحات لوحة التحكم للمسؤولين والعيادات، بما في ذلك إدارة المواعيد، الفواتير، المرضى، والتقارير.
*   `components/`: مكونات React القابلة لإعادة الاستخدام.
*   `database/`: ملفات خاصة بقاعدة البيانات، بما في ذلك الهجرات (migrations).
    *   `migrations/`: تحتوي على ملفات SQL لتغييرات قاعدة البيانات.
*   `lib/`: مكتبات ووظائف مساعدة.
*   `public/`: الأصول الثابتة (مثل الصور).
*   `tests/`: ملفات الاختبار.
*   `types/`: تعريفات TypeScript.

## 3. التقنيات المستخدمة

بناءً على ملف `package.json`، يعتمد المشروع على التقنيات الأساسية التالية:

*   **Next.js**: إطار عمل React لتطبيقات الويب، يدعم العرض من جانب الخادم (SSR) وتوليد المواقع الثابتة (SSG).
*   **React**: مكتبة JavaScript لبناء واجهات المستخدم.
*   **TypeScript**: مجموعة شاملة من JavaScript تضيف أنواعًا ثابتة.
*   **Tailwind CSS**: إطار عمل CSS قائم على المرافق لتصميم سريع لواجهة المستخدم (مفترض من خلال `devDependencies`).
*   **Clerk/nextjs**: لإدارة المصادقة والمستخدمين.
*   **Supabase/supabase-js**: للتعامل مع قاعدة البيانات والخدمات الخلفية (مثل المصادقة والتخزين).
*   **Resend**: لإرسال رسائل البريد الإلكتروني.
*   **PostgreSQL**: قاعدة البيانات العلائقية (مفترض من خلال استخدام Supabase وملفات SQL).

## 4. تحليل قاعدة البيانات (Schema)

تعتمد قاعدة البيانات على `PostgreSQL` وتستخدم `UUIDs` كمفاتيح أساسية لمعظم الجداول. تم تصميم الـ Schema لدعم نظام متعدد المستأجرين (multi-tenant) حيث يتم ربط معظم البيانات بـ `clinic_id`. تتضمن قاعدة البيانات الجداول الرئيسية التالية:

*   **roles**: تعريف أدوار المستخدمين (admin, clinic_owner, doctor, staff, patient) مع صلاحيات JSONB.
*   **clinics**: الكيان الرئيسي للعيادات، يحتوي على معلومات الاتصال، حالة الاشتراك، والحدود القصوى للمستخدمين والمرضى.
*   **users**: مستخدمو النظام، مرتبطون بـ `auth.users` في Supabase للمصادقة، ويرتبطون بـ `clinic_id` و `role_id`.
*   **doctors**: معلومات الأطباء، مرتبطة بـ `clinic_id` و `user_id`.
*   **patients**: سجلات المرضى، مرتبطة بـ `clinic_id`، تتضمن معلومات شخصية، تاريخ طبي، ومعلومات تأمين.
*   **appointments**: جدولة المواعيد، مرتبطة بـ `clinic_id`, `patient_id`, و `doctor_id`.
*   **treatments**: سجلات العلاج، مرتبطة بـ `clinic_id`, `appointment_id`, `patient_id`, و `doctor_id`.
*   **invoices**: سجلات الفواتير، مرتبطة بـ `clinic_id` و `patient_id`.
*   **payments**: سجلات الدفع، مرتبطة بـ `clinic_id`, `invoice_id`, و `patient_id`.
*   **subscriptions**: إدارة اشتراكات العيادات، مرتبطة بـ `clinic_id`.
*   **activity_logs**: سجلات التدقيق لتتبع الأنشطة في النظام.

### الميزات الرئيسية لقاعدة البيانات:

*   **Multi-Tenancy**: يتم تطبيق مفهوم Multi-Tenancy بشكل صارم من خلال ربط معظم الجداول بـ `clinic_id`، مما يضمن عزل البيانات بين العيادات المختلفة.
*   **Row Level Security (RLS)**: تم إعداد وظائف مساعدة (helper functions) لـ RLS (مثل `get_user_clinic_id()`, `is_super_admin()`) مع `SECURITY DEFINER` لفرض أمان البيانات على مستوى الصفوف.
*   **Audit Logging**: يتم تسجيل جميع عمليات `INSERT`, `UPDATE`, `DELETE` على الجداول الرئيسية في جدول `activity_logs`، مع منع التكرار (recursion prevention) وتضمين القيم القديمة والجديدة.
*   **Soft Deletes**: معظم الجداول تحتوي على عمود `deleted_at`، مما يشير إلى استخدام الحذف الناعم بدلاً من الحذف الفعلي للبيانات.
*   **Unique Constraints**: تم تعديل القيود الفريدة (unique constraints) لتكون على مستوى العيادة (clinic-scoped) بدلاً من أن تكون عامة، باستثناء بعض الحقول مثل `doctors.license_number` و `clinics.email`.
*   **Updated_at Triggers**: يتم تحديث عمود `updated_at` تلقائيًا في معظم الجداول عند كل عملية تحديث باستخدام `TRIGGER`.
*   **Indexes**: تم إنشاء فهارس (indexes) متعددة لتحسين أداء الاستعلامات، بما في ذلك الفهارس المركبة (composite indexes).

## 5. الخلاصة

مشروع `Omar Clinic Pro` هو تطبيق ويب متكامل لإدارة العيادات، مبني على `Next.js` و `Supabase`. يتميز المشروع بهيكل منظم وقاعدة بيانات مصممة بعناية لدعم Multi-Tenancy، أمان البيانات (RLS)، وتتبع الأنشطة (Audit Logging). الـ Schema قوية وتتضمن آليات للحذف الناعم وتحسين الأداء. هذا التحليل يوفر أساسًا متينًا لمواصلة العمل على المشروع وتطويره.
