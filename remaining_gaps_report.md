# تقرير النواقص والملاحظات المتبقية - مشروع Omar Clinic Pro

بعد إجراء فحص شامل لقاعدة البيانات والكود المصدري لمشروع **Omar Clinic Pro**، تم تحديد مجموعة من النواقص البرمجية والملاحظات التي تحتاج إلى استكمال أو إصلاح لضمان عمل النظام بكفاءة وبشكل متكامل.

## 1. نواقص الواجهات (UI/Frontend) 🖥️

*   **صفحة المخزون (Inventory)**: تم ذكر المخزون في الروابط الجانبية (`Sidebar` و `Layout`) ولكن لا توجد صفحة فعلية (`app/dashboard/clinic/inventory/page.tsx`) ولا توجد جداول في قاعدة البيانات تدعم المخزون.
*   **صفحة الأطباء (Doctors Management)**: يوجد دعم برمجي للأطباء في `supabase.ts` (`doctorsDbHelpers`) ويوجد جدول في قاعدة البيانات، لكن **لا توجد واجهة لإدارة الأطباء** في لوحة التحكم (`app/dashboard/clinic/doctors/page.tsx` غير موجودة).
*   **صفحة الفواتير (Billing/Invoices)**: الرابط في الشريط الجانبي يشير إلى `/dashboard/clinic/invoices`، ولكن تم العثور على أخطاء سابقة عند محاولة الوصول إلى `billing/page.tsx` (تم التأكد من وجود `invoices` ولكن يجب التحقق من عدم وجود روابط مكسورة تشير لـ `billing`).
*   **إدارة الموظفين (Staff Management)**: لا توجد واجهة لإدارة الموظفين الآخرين (مثل موظفي الاستقبال والمحاسبين) رغم وجود أدوار مخصصة لهم في `roles.ts`.
*   **صفحات الأدمن (Super Admin)**: معظم صفحات الأدمن (مثل `users`, `clinics`, `reports`, `settings`, `subscriptions`) تحتوي فقط على رسالة "قريباً..." وتحتاج إلى بناء فعلي.
*   **الوضع الليلي (Dark Mode)**: المكونات تدعم الثيمات ويوجد `ThemeToggle`، لكنه غير مدمج بشكل واضح في الشريط الجانبي (`Sidebar`) ليستخدمه المستخدم.

## 2. نواقص قاعدة البيانات (Database/Backend) 🗄️

*   **جدول الوصفات الطبية (Prescriptions)**: تم ذكر الوصفات الطبية في ملفات الـ API (مثل إشعارات الواتساب والبريد) وفي ملفات RLS (`rls_patient_portal.sql`)، ولكن **لا يوجد جدول للوصفات الطبية** في `001_initial_schema.sql` أو في أي Migration آخر.
*   **جدول المخزون (Inventory)**: لا يوجد أي جدول متعلق بالمخزون في قاعدة البيانات.
*   **ربط `clerk_id`**: يتم استخدام `clerk_id` في سياسات RLS الخاصة ببوابة المريض (`rls_patient_portal.sql`) وفي الـ API (`app/api/portal/data/route.ts`)، ولكن **العمود `clerk_id` غير موجود** في جداول `patients` أو `doctors` في المخطط الأولي (`001_initial_schema.sql`).
*   **تحديث الـ Schema**: هناك حاجة لإضافة عمود `shopify_subscription_id` إلى جدول `subscriptions` حيث يتم استخدامه في الـ Webhook الخاص بـ Shopify (`app/api/webhooks/shopify/route.ts`).

## 3. نواقص التكامل والخدمات الخارجية (Integrations) 🔗

*   **إشعارات WhatsApp**: الكود الحالي في `lib/notifications.ts` و `app/api/notifications/whatsapp/route.ts` هو مجرد Mock (كود وهمي) يحتاج إلى ربط حقيقي مع مزود خدمة مثل Twilio أو Ultramsg.
*   **إشعارات البريد (Resend)**: الكود جاهز، ولكن يجب التأكد من إعداد قوالب البريد بشكل صحيح وربط الـ API Key في بيئة الإنتاج.
*   **تكامل Asana**: تم ذكره في تقارير سابقة، ولكن لا يوجد أي أثر لكود يربط النظام بـ Asana.
*   **رفع الملفات (Storage)**: سياسات الـ Storage موجودة (`setup_storage.sql`)، لكن يجب التأكد من إنشاء الـ Buckets فعلياً في Supabase وربطها بشكل صحيح مع مكون الرفع (`MedicalFileUpload.tsx`).

## 4. ملاحظات الأمان والصلاحيات (Security & Auth) 🔒

*   **إدارة الأدوار (Role Management)**: يتم تحديد الأدوار في `lib/roles.ts` و `api-permissions.ts`، ولكن يجب التأكد من مزامنة هذه الأدوار بشكل صحيح مع `publicMetadata` في Clerk عند إنشاء أو تحديث المستخدمين.
*   **التحقق من الاشتراك (Subscription Enforcement)**: يتم التحقق من الاشتراك في `middleware.ts` والـ APIs، ولكن يجب سد أي ثغرات تسمح بالوصول للبيانات عبر الـ API مباشرة إذا كان الاشتراك منتهياً (RLS policies قد تحتاج لتحديث لتشمل حالة الاشتراك).

## التوصيات وخطوات العمل القادمة 🚀

1.  **إنشاء جداول قاعدة البيانات المفقودة**: `prescriptions`، `inventory`، وتحديث جدولي `patients` و `doctors` لإضافة `clerk_id`.
2.  **بناء واجهات الإدارة المفقودة**: `doctors`، `inventory`، `staff`، واستكمال صفحات الـ `admin`.
3.  **إضافة تبويب الوصفات الطبية والملفات**: في صفحة تفاصيل المريض (`/patients/[id]`) لعرض الوصفات وإدارتها.
4.  **تفعيل خدمات الطرف الثالث**: ربط WhatsApp API بشكل فعلي، والتأكد من إعدادات Resend و Shopify Webhooks.
